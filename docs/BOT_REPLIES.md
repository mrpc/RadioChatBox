# Fake User Auto-Replies (LLM Bots)

Fake users can answer private messages on their own through an LLM (DeepSeek by
default). The goal is a conversation that reads like a real person: a short
pause before replying, a realistic typing delay, casual Greek/greeklish, a
limited number of messages, and a natural exit.

Everything is off by default. Nothing is sent to any external API until an admin
enables it and provides an API key.

---

## How it works

```
User sends a DM to a fake user
        │
        ▼
public/api/private-message.php
  stores the message, then queues a `bot_reply` job
  (reading delay: 2-8s by default)  ── nothing blocks the HTTP response
        │
        ▼
bot-worker.php  →  BotService::processReplyJob()
  ├─ message budget spent?  → ask the LLM for a CLOSING message
  │                           (falls back to a random canned goodbye on failure)
  └─ otherwise             → ask the LLM for a normal reply
        │
        │  reply text is sanitised, then a `bot_deliver` job is queued
        │  with a typing delay of ~1.5s per word
        ▼
bot-worker.php  →  BotService::processDeliverJob()
  INSERT INTO private_messages + PUBLISH to Redis
  → the recipient's SSE stream shows the message
```

The delay between the LLM answering and the message appearing is what makes the
bot look like it is typing: the row is not written to the database until the
delay has passed, so nothing shows up early in a page reload either.

### Conversation limit and the exit message

Each `(fake user, real user)` pair has a message budget (`bot_max_messages_per_thread`,
default **4**). Once the bot has sent that many messages in a thread, its next
message is a **closing message** and it then stays silent for good.

That closing message is still generated by the LLM, with an extra instruction
appended to the system prompt, so the goodbye refers to whatever was actually
being discussed instead of always being the same sentence. If that request
fails, a random line from the fallback list is used — there are 50+ built-in
Greek/greeklish variants (`BotService::DEFAULT_FAREWELLS`), so conversations
still don't all end identically.

### Admin takeover stops the bot

Opening a conversation in the **Impersonate** tab marks that thread as taken
over, and so does sending a message as the fake user. From that point the bot
never replies in that thread — including a reply that is already queued or has
already been generated, because every stage re-checks the state before writing.

The takeover is per conversation, not per bot: the same fake user keeps
auto-replying to everyone else. Use **🤖 Back to bot** in the conversation
header to hand it back, or **♻️ Back to bot + reset** to also clear the message
budget.

---

## Setup

### 1. Run the migration

```bash
psql "$DATABASE_URL" -f database/migrations/023_add_bot_replies.sql
```

### 2. Configure it in the admin panel

**Settings → 🤖 Fake User Auto-Replies (Bots)**

| Setting | Default | Notes |
|---|---|---|
| Enable auto-replies globally | off | Master switch. When off, no LLM request is ever made. |
| DeepSeek API Key | – | From [platform.deepseek.com](https://platform.deepseek.com/). Stripped from the public settings payload. |
| API Base URL | `https://api.deepseek.com` | Any OpenAI-compatible endpoint works. |
| Reasoning | off | The v4 models reason internally; on a one-line reply that spent ~175 tokens (and the whole budget when it was 300) for no benefit. |
| Model | `deepseek-v4-flash` | Dropdown built from `BotService::availableModels()` — the API rejects unknown names, so the valid ones live in one place. A value already stored for a custom endpoint is kept as an extra option. |
| Temperature | `1.3` | DeepSeek's own recommendation for casual conversation. |
| Max tokens per reply | `1000` | Covers internal reasoning too. Too low and the reply is cut off mid-sentence. |
| Message limit per conversation | `4` | Then the closing message is sent. |
| Conversation history | `20` | Messages sent as context. |
| Conversation context | built-in | Prefilled with the built-in text so it is visible and editable; empty falls back to the built-in one. |
| Closing instruction | built-in | Appended to the prompt for the last message. |
| Fallback goodbye variants | built-in (50+) | One per line; used only if the closing request fails. |
| Typing speed | `1.5` s/word | Clamped by min/max typing delay (`2`–`45`s). |
| Reading delay | `2`–`8`s | Random pause before the bot starts working. |

The API key can also come from `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` /
`DEEPSEEK_MODEL` in `.env` as a fallback for deployments that keep secrets
outside the database. Admin settings take precedence when set.

### 3. Enable it per fake user

**Fake Users → 🤖 Bot** on the row you want:

| Field | Notes |
|---|---|
| Reply automatically | Per-bot switch. |
| Personality / interests | Appended to the generated prompt (name, age and sex are added automatically). |
| Custom system prompt | Replaces the generated prompt entirely. |
| Message limit | Per-bot override. Empty = global. |
| Typing speed | Per-bot override. Empty = global. |
| Fallback goodbye variants | Per-bot override, one per line. Empty = global. |

A fake user must be **active** *and* have the bot enabled to auto-reply.

### 4. Run the worker

The worker is what actually calls the LLM and delivers the messages. Without it
nothing is ever sent.

```bash
php bot-worker.php status     # worker health + queue depth + configuration
php bot-worker.php run        # loop, polling every second (recommended)
php bot-worker.php once       # process what is due, then exit
php bot-worker.php flush      # drop every queued job
```

**systemd** (`/etc/systemd/system/radiochatbox-bot.service`):

```ini
[Unit]
Description=RadioChatBox bot worker
After=network.target

[Service]
Type=simple
WorkingDirectory=/path/to/radiochatbox
ExecStart=/usr/bin/php /path/to/radiochatbox/bot-worker.php run
Restart=always
RestartSec=5
User=www-data

[Install]
WantedBy=multi-user.target
```

**Cron-only alternative.** `run --max-runtime=N` is a real loop, not a single
pass: it polls every `--sleep` seconds (default 1s) until the runtime is up. So a
minutely cron job that runs for 55s covers almost the whole minute at
~1-second accuracy — the only dead time is the ~5s gap before the next run
starts:

```cron
* * * * * cd /path/to/radiochatbox && php bot-worker.php run --max-runtime=55 >> logs/bot-worker.log 2>&1
```

No `flock` wrapper is needed — the worker takes its own lock (below). Note that
**`once` is the minute-accurate option**, not this one: it processes whatever is
due and exits, so a job that becomes due 5 seconds later waits for the next cron
tick. Use it when you want the shortest-lived process and don't mind the delay.

Flags for `run`: `--max-runtime=N` (0 = forever), `--sleep=N` (poll interval,
default 1s), `--batch=N`, `--stale-after=N`, `--lock=PATH`, `--verbose` (also log
skipped jobs). `SIGTERM`/`SIGINT` finish the current batch before exiting, so
restarts don't cut a job in half.

### Single instance, heartbeat, and stuck workers

`run` and `once` hold a lock so a second worker never doubles up — whether it
comes from cron, systemd or a manual run. It is implemented in plain PHP
([src/WorkerLock.php](../src/WorkerLock.php)): the lock is a small JSON file
whose *creation* is the atomic step (`fopen($path, 'x')` → `O_CREAT|O_EXCL`),
and the same file carries the heartbeat.

> `flock()` is deliberately not used: on some filesystems — notably Docker bind
> mounts on macOS — it silently does nothing, and two processes both "acquire"
> the same lock with no error. Atomic create works everywhere.

The lock file (`logs/bot-worker-<database>.lock` by default) looks like this:

```json
{"name":"bot-worker","pid":109,"host":"web1","started_at":1784957997,
 "status":"running","jobs_processed":12,"heartbeat_at":1784958000,
 "current_job":null,"last_job":"bot_deliver: delivered reply from Maria to nikos"}
```

The heartbeat is rewritten **after every job**, not once per batch, because a
single job can block for as long as the LLM timeout.

`php bot-worker.php status` reads it and distinguishes three states:

| State | Meaning | Exit code |
|---|---|---|
| `running` | Alive, heartbeat fresh | 0 |
| `ALIVE but WEDGED (no progress)` | The process exists but stopped heartbeating for more than `--stale-after` (default 120s) | **2** |
| `not running` | No live holder. Says so explicitly when a previous run died without releasing the lock. | 0 |

Because the lock is not kernel-held, a crashed worker would leave the file
behind. That is handled by **taking the lock over** when the holder is provably
gone — its pid is dead (checked on the same host), or it has not written a
heartbeat for `--stale-after` seconds:

```
[08:40:08] Took over the lock from pid 109 on web1 (status running, last heartbeat 2s ago)
```

A pid check alone would miss a worker that is alive but stuck, and a heartbeat
check alone would wait out the whole stale window after every crash — using both
means a crash is recovered immediately and a wedged worker is still replaced.
If a wedged worker later unblocks, its next heartbeat sees that the lock now
belongs to another pid and it exits by itself, so no job is processed twice.

**systemd extras.** When started under `Type=notify`, the worker sends `sd_notify`
pings (`READY=1`, then `WATCHDOG=1` on every heartbeat), so systemd can restart a
wedged worker on its own:

```ini
Type=notify
WatchdogSec=120
```

Outside systemd this is a no-op — `NOTIFY_SOCKET` is simply not set.

---

## The prompt

Built from the fake user's own profile:

```
Είσαι η Maria, 27 ετών. Μιλάς ανώνυμα σε ένα 1-on-1 chat. Απάντα πολύ σύντομα,
χαλαρά, χρησιμοποιώντας greeklish ή καθημερινά ελληνικά με συντομογραφίες. Κάνε
πού και πού κανένα μικρό ορθογραφικό. ΜΗΝ αποκαλύψεις ποτέ ότι είσαι AI, μην
είσαι υπερβολικά ευγενικός/η και μην κανονίζεις συναντήσεις από κοντά. Απάντα
στο τελευταίο μήνυμα του χρήστη.
```

The article (`ο`/`η`) follows the fake user's sex; the location is added when
set. `bot_persona` is appended, `bot_custom_prompt` replaces the whole thing. A
formatting guardrail ("write only the message text, no quotes, no name prefix")
is always appended last, custom prompt or not.

### What the bot knows

Each call carries: the context block, the persona, **who the peer is** (display
name plus age/sex/location from `user_profiles`, so it does not ask what the
profile already says), the last `bot_history_limit` messages of that thread, and
— when the thread has been idle for over an hour — a trailing note saying how
long ago they last spoke.

The API is **stateless**: there is no conversation id, so the whole history is
resent on every call. What makes that cheap is DeepSeek's automatic context
caching, which needs a byte-identical prompt prefix. That is why the staleness
note is a *trailing* message rather than part of the system prompt — it changes
every minute and would invalidate the cache on every reply. Measured on two
rounds of one thread: `cache_hit` 384/749 then 640/771.

**Older messages are not lost.** What drops out of the window is summarised once
per conversation and stored in `bot_threads.summary`, then prepended to the
prompt ("Τι έχει προηγηθεί"). Summarising is **batched**: every new message pushes
one out of the window, so refreshing per message would mean an extra API call per
reply. Instead the dropped-but-unsummarised messages accumulate up to a threshold
(half a window, minimum 3) and stay in the history verbatim until then, so
nothing is dropped while a summary is pending. A failed summary never costs the
user their reply.

Because the summary only changes when the window slides, it sits in the *cached*
part of the prompt. It can be switched off, and its instruction customised, in
Settings → Conversation, and the current summary is shown in the Impersonate
header ("📝 Remembers: ...").

### Conversation context

The persona alone is not enough: the model reads openers literally and answers
the wrong question. `BotService::DEFAULT_CONTEXT_PROMPT` is prepended to every
prompt and explains the setting plus the phrases that are actually used —
notably that **"είσαι ελεύθερη;" asks about relationship status, not about
having time to chat** — along with "τι κάνεις;", "από πού είσαι;", "ασλ" and
"τι ψάχνεις;". It also tells the bot to turn down photo, camera and phone-number
requests with a human excuse rather than admitting it technically cannot.

The `bot_context_prompt` setting replaces that block. The admin panel prefills
the field with the built-in text (with a "Reset to built-in" link), so what the
bots actually receive is visible and can be adjusted instead of guessed; leaving
it empty falls back to the built-in one. Order in the final prompt: context →
persona → formatting guardrail.

Model output is then cleaned up: wrapping quotes removed, a leading `Name:`
prefix removed, `<thinking>`-style blocks dropped **with their contents**,
stray markup stripped, blank lines collapsed, truncated to the chat's message
length limit, and finally passed through the same `MessageFilter` as human
messages.

---

## Data model

`fake_users` gains: `bot_enabled`, `bot_persona`, `bot_custom_prompt`,
`bot_max_messages`, `bot_typing_seconds_per_word`, `bot_farewell_messages`.

`bot_threads` — one row per `(fake_user_id, peer_username)`:

| Column | Meaning |
|---|---|
| `messages_sent` | Messages the **bot** authored (admin impersonation replies are not counted) |
| `is_taken_over` / `taken_over_at` / `taken_over_by` | Admin took over this conversation |
| `farewell_sent_at` | Closing message sent — the bot never replies here again |
| `last_reply_at`, `last_error` | Diagnostics, surfaced in the Impersonate tab |

### Queue

Redis-backed delayed queue (`src/JobQueue.php`):

```
<prefix>jobs:delayed   ZSET   jobId => run-at timestamp
<prefix>jobs:data      HASH   jobId => JSON payload
```

Claiming is atomic per job (only the worker whose `ZREM` removed the id
processes it), so several workers can run side by side. Failed jobs are retried
up to 3 times with backoff; a failed *closing* message is not retried — it falls
back to a canned variant so the conversation still closes.

`<prefix>bot:epoch:<fake_user_id>:<md5(peer)>` is bumped on every inbound
message and on takeover. Jobs carry the epoch they were created with, so a newer
message (or a takeover) silently invalidates older queued work — the bot answers
the current state instead of replying twice.

---

## Safety and cost notes

- **Nothing runs until enabled.** Master switch off, per-bot switch off, and no
  API key all independently prevent any external request.
- **The goodbye is the only LLM call beyond the budget.** After
  `farewell_sent_at` the thread makes no further requests at all.
- **Bots never talk to each other** — a message from another fake user is
  ignored, so there is no loop.
- **Blocks are respected**: DM blocks are enforced before the message is stored,
  so a blocked user never triggers a bot reply.
- **The API key never reaches the frontend.** `getPublicSettings()` strips every
  `bot_*` key; only the authenticated admin API returns it.
- Each reply costs one LLM request. With the default budget, a conversation
  costs at most 5 requests (4 replies + 1 closing message).

## Diagnostics

Every LLM call is logged to `bot_llm_log` (request, reply, `finish_reason`, token
usage including reasoning tokens, duration, error), which is what turned "replies
are cut off" from guesswork into a one-line answer.

```bash
php bot-worker.php log              # recent calls + a 24h summary
php bot-worker.php log --problems   # only failures and truncations
php bot-worker.php prune-log        # drop entries past the retention window
```

The dashboard shows a **Bot LLM Tokens (24h)** card while auto-replies are on,
including a truncation and error count. Retention defaults to 7 days and logging
can be switched off in Settings → Diagnostics.

To retest a bot from scratch, use **🧹 Clear conversations** in its bot dialog
(or the 🧹 button in the Fake Users row): it deletes that bot's private messages,
its per-thread budget and takeover state, and any queued reply.

## Troubleshooting

| Symptom | Check |
|---|---|
| Replies cut off mid-word / incoherent | `php bot-worker.php log --problems`. A `finish_reason` of `length` means the token budget ran out — raise **Max tokens** or leave **Reasoning** off. |
| No replies at all | `php bot-worker.php status` — enabled? key set? worker running? |
| `ALIVE but WEDGED` in `status` | The worker is stuck (usually a hung HTTP connection). Kill it; the next run takes the lock over on its own. |
| "Another worker is already running" | Expected — the lock is doing its job. `status` shows which pid holds it. |
| Replies but never delivered | Worker died between the two job stages; check the worker log. |
| "conversation taken over by admin" in the log | Expected after impersonating. Use **Back to bot**. |
| Bot went silent after 4 messages | Working as intended. **Back to bot + reset** clears the budget. |
| `last_error` in the Impersonate header | The LLM call failed (bad key, rate limit, network). |
